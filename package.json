{
  "name:": "cypress-api",
  "scripts": {
    "test": "cypress open",
    "cy_run_dev": "npx cypress open --env username=test@example.com,password=password234",
    "cy_run_prod": "npx cypress open --env username=$USERNAME,password=$PASSWORD"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "cypress": "^15.10.0"
  }
}
